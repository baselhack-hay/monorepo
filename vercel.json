{
  "version": 2,
  "scope": "my_own_vercel_scope",
  "github": {
    "enabled": false
  },
  "installCommand": "npm i",
  "buildCommand": "ls && pwd && cd packages/frontend && npm run build && cd ../backend && npm run build",
  "builds": [
    {
      "src": "/dist/packages/backend/main.ts",
      "use": "@vercel/node"
    },
    {
      "src": "/dist/packages/frontend/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/dist/packages/backend/main.ts" },
    { "handle": "filesystem" }
  ]
}
